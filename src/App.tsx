// Packages
import React from "react";

// Components
import { List } from "./components";

const App: React.FC = () => {
  // -------------------------------------------------
  // Render
  // -------------------------------------------------
  return <List initialItems={["Luiz", "Henrique", "de"]} />;
};

export default App;
